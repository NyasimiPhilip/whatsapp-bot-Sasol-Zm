
OPENAI_API_KEY=

# Authorized Phone Numbers (comma-separated list)
# Only these numbers can use admin commands: !start, !stop, !check
# Format: +27123456789,+27987654321 (include country code with +)
# REQUIRED: Bot admin commands will not work without at least one authorized number
AUTHORIZED_NUMBERS=
